userEmail: local.guy@nowhere.near
userAccountNb: uid1000
logFileDir: log
submitExec: bash
mpiExec: echo
resourceManager: bash
scriptPattern: |
    #!/bin/bash
    # -queue {queue}
    # -nproc {nproc}
    # -time {hours}:{minutes}:{seconds}
    # -jobName {jobName}
    # -logFile {logFile}
    # -userEmail {userEmail}
    # -userAccountNb {userAccountNb}
    # -parentJobAny {parentJobAny}
    # -parentJobOk {parentJobOk}
