userEmail: glassJoe@stccmop.org
userAccountNb: j007
logFileDir: /tmp/logs
submitExec: qsub
mpiExec: mpirun -n {nthread}  # number of threads will be substuted
nCoresSocket: 4  # TODO are these needed?
nCoresNode: 8  # TODO are these needed?
resourceManager: sge
scriptPattern: |
    #!/bin/bash
    #$ -cwd
    #$ -N {jobName}
    #$ -o {logFile}.out
    #$ -e {logFile}.err
    #$ -q {queue}
    #$ -pe orte {nproc}
    #$ -M {userEmail}
    #$ -A {userAccountNb}
    #$ --dependency=afterany:{parentJobAny}
    #$ --dependency=afterok:{parentJobOk}
    #$ -m ea
    #$ -j
    #$ -V
