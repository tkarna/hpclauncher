userEmail: glassJoe@stccmop.org
userAccountNb: j007
logFileDir: log
submitExec: qsub
mpiExec: mpirun -pe orte {nproc}  # number of procs will be substuted
nCoresSocket: 4  # TODO are these needed?
nCoresNode: 8  # TODO are these needed?
resourceManager: sge
scriptPattern: |
    #!/bin/bash
    #$ -cwd
    #$ -N {jobName}
    #$ -o {logFile}.out
    #$ -e {logFile}.err
    #$ -q {queue}
    #$ -M {userEmail}
    #$ -A {userAccountNb}
    #$ --dependency=afterany:{parentJobAny}
    #$ --dependency=afterok:{parentJobOk}
    #$ -m ea
    #$ -j
    #$ -V
